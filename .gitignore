# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment and secrets
.env
.env.*
*.pem
*.jks
*.keystore

# Firebase config (contains API keys)
android/app/google-services.json
ios/App/App/GoogleService-Info.plist

# Build outputs
*.apk
*.aab
*.ipa
android/app/build/
android/build/
android/.gradle/
ios/build/

# Contraseñas de firma (la plantilla .example sí se versiona)
android/keystore.properties

# -- Secretos que se descargan a la raiz y no deben salir de aqui -------------
# El 11/09/2026 una clave de cuenta de servicio de Firebase y unos codigos de
# recuperacion acabaron en un commit por estar sueltos en la raiz. El push lo
# bloqueo GitHub, pero el repositorio es publico y no puede depender de eso.
*firebase-adminsdk*.json
*-service-account*.json
recovery-codes.txt